# JSON Format

**Change from YAML:** Converted to strict JSON format with explicit object structure.

- Element-specific slide patterns (title_card, definition, in_practice, key_distinctions)
- Nested objects with `{}` braces
- Key-value pairs with `:` colon
- All keys quoted as strings
- Metadata as `_comment` fields
- Machine-parseable with rigid syntax
