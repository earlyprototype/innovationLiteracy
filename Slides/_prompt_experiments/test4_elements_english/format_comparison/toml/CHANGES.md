# TOML Format

**Change from YAML:** Converted to TOML format with explicit sections using `[section.subsection]` syntax.

- Element-specific slide patterns (title_card, definition, in_practice, key_distinctions)
- Key-value pairs with `=` assignment
- Nested sections with dot notation
- Quoted strings for all values
- Comments preserved with `#`
